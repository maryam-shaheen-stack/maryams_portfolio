# Copy this file to `.env` (local dev) or set as an environment variable
# on your hosting platform (Vercel/Netlify) for production.

# Base URL of the deployed backend API, including the /api prefix.
# Local dev (matches backend/.env.example PORT=5000):
VITE_API_URL=http://localhost:5000/api
